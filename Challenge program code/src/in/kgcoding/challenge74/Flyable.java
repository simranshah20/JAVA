package in.kgcoding.challenge74;

/**
 * Flyable interface defines a contract for flying behavior.
 * Any class implementing this interface must provide an implementation of the fly method.
 */
public interface Flyable {
    /**
     * Abstract method to define flying behavior.
     * Classes implementing this interface must override this method to specify how they fly.
     */
    void fly();
}
